从9月2号开始腾讯的WXG一面，到9月14号的HR面结束，终于又完成了一个公司的面试，对于0offer的我来说也确实是一场莫大的慰藉吧~
整体来说比较难，前三轮中八股文比较少（携程一面全是八股文，把我背的累死），因为我对C++/go不太熟悉，所以语言方面问的比较少，更多的是算法和计算机网络，操作系统，以及各种各样的场景题

### 一面
#### 基础
1. vector申请堆内存的方式
这个不会。。。（只记得如果是小内存的话直接申请，如果是大内存大的话用mmap来申请）
红黑树的结构
2. 进程通信方式
  * 共享内存（重点介绍）
  * 管道（有名管道和无名管道）
  * 信号量
  * socket
  * 消息队列
3. UDP和TCP的区别
可靠，面向连接，字节流和数据报，应用场景
3. MySQL主从同步的通信方式
4. MySQL的事物
ACID，事物特性的实现方式等
#### 设计
1. 一个订单表，如何通过用户和商家维度分库分表

  除了冗余两份表还可以怎么做

2. Redis扣库存如何保证幂等性

### 二面
1. 一致性问题

  最终一致性和强一致性，分别有哪些手段来保证

2. 如何看待BAT

3. 很多很大的文件从机器A拷贝到机器B

4. 实现微信公众号的关注和被关注的数据库系统设计
  两个表，缓存，count（异步，怎么解决大量写，缓存）
  （PS：这个地方聊了近30分钟）
####  算法
1. 逆序数组
2. 合并有序链表
3. 不转字符串拿到数字中0的个数（通过%和/）


### 三面

> 三面让自己发现了一些不足，就是没有一种非常精通的东西

1. 说一下DUBBO/HSF/任何一个精通的中间件
这里我说的是在阿里实习的时候的一个基于数据库补偿机制来保证最终一致性的一个框架（阿里答辩也说的这个，对它研究的非常透彻/笑哭）
2. Maven的打包过程
3. 如何保证库存的幂等
通过订单，订单和库存不在一个DB中怎么保证一致性
4. TCP的三次握手和四次挥手，流量控制和拥塞控制
5. raft和paxos
#### 算法
1. 两数之和（保存进位）
2. 最长不重复子串(dp)
3. 翻转K个链表(递归）
4. 数组右移K位(时间n,空间1)

### 四面

> 四面问的问题很基础，大概问了30分钟，然后就是做题（下面的问题没有记太全）
1. TCP和UDP区别

2. TCP的拥塞控制

3. Jvm的内存分配

4. 10亿个数字求中位数

5. 项目问题

6. 10亿个数字找到中位数

  10亿个数字是4GB，如果内存足够的话，可以对他们进行一次快排，找到对应中位数
  如果内存不够的话，可以采取分片的思路。把一部分数据先捞进内存，然后根据数值范围（无符号整数是0-2^32-1）划分，统计数值范围内数字出现的次数，当统计完之后，对数值范围重新排序，直至找到中位数
  (这种题型也是屡见不鲜，还有topK（最大/小）问题，还有出现最多的)

#### 算法

1. 一个数量为N的整数数组，其中有很多重复，其中一个数字出现次数超过N/2，请将该数字找出来（百度三面算法原题）

2. 给定一个无序的整数数组，求出数组中的最长递增子序列的长度；
例如， {7, 2, 3, 1, 5, 8, 9, 6}，最长递增子序列为{2, 3, 5, 8, 9}，长度为5

3. 假设c库中有函数rand()能够生成均匀随机的0 - 65535的随机数，现有30万员工参与抽奖，请使用rand()函数编写一个完整的可执行程序，从这30万员工中随机抽取10万名中奖员工

4. 在一个排列中，如果一对数的前后位置与大小顺序相反，即前面的数大于后面的数，那么它们就称为一个逆序。一个排列中逆序的总数就称为这个排列的逆序数。如2431中，21，43，41，31是逆序，逆序数是4。对于数字序列268941，因为对于4的右边比它小的数字只有1个，对于9的右边比它小的数字只有2个，对于8的右边比它小的数字只有2个，……，对于2的右边比它小的数字只有1个，所以序列268941的逆序数等于1+2+2+2+1 = 8。
设计求逆序数的算法，最快的时间复杂度是多少？具体算法描述？


### 面委会

1. 实习时的业务和IO
2. 压缩算法/查找算法
3. Redis的索引
4. OS的内存分配，回收
5. 52张牌抽五张，2张一样，3张一样
A(13,2)C(4,2)C(4,3)/C(52,5)
6. 矩阵最小路径和
给定一个 n * m 的矩阵 a，从左上角开始每次只能向右或者向下走，最后到达右下角的位置，路径上所有的数字累加起来就是路径和，输出所有的路径中最小的路径和。


### HR面

1. 自我介绍（重点不是项目和技能，体现软实力）
2. 在公司实习一个最难的地方
3. 自己的优缺点
4. 父母同意到广州吗，自己家是哪里的
5. 为什么转专业
6. 为什么写日记，什么时候开始的
7. 未来有什么打算
8. 对微信哪些部门有意向
9. 为什么学计算机，为什么选择后端