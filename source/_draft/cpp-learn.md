---
title: "C++学习"
tags:
   - C++
categories:
   - 基础夯实
date: 2020-10-21 10:36
---

C++相关语法和概念的学习~

<!--more-->

### 预处理和头文件

### 编译阶段

分为编译和link

### 指针和引用

pointer只是一个地址，在32位机器中一个指针4字节，在64位机器中，一个指针8字节。地址中存储中对应的变量。

指针的类型表示该指针指向的变量的类型，这是人为规定的，告诉编译器应该如何解析指针指向的那一串数字

```c++
// c中的函数是值传递，通过copy获得新的一份
void increase(int* ptr) {
    (*ptr) ++;
}
increase(&a);
```

引用和指针其实是一会事，引用不能为0，因为要求引用引用实体。引用其实是一种代称，它没有实际的内存地址。

当引用建立后，即一直是某一变量的引用，不能修改。譬如，`int& ref = a`，那么ref就永远是a的变量

```
void increase(int& ref) {
	ref++;
}
increse(a);
```

在工程中，我们常需要改变某些变量，一般都是通过引用传递，这和Java中类的引用差不多

### 常见关键字

inline,static,nullptr

static：类或者struct外的static在link阶段是局部的，意思就是在link阶段，其他编译unit不能访问该单元的static变量或方法。类或者struc内的static表明这部分内存区域是被所有instance共享的

### 面向对象

#### 类和结构体的区别

最大的区别就是strut是公有的，想要私有必须声明。类是私有的，想要公有必须声明。

C++存在struct的最大原因，就是C++想要和C进行兼容（当然我们可以通过#define进行替换）

#### 枚举

只是一个整型

#### 继承

#### 纯虚函数和虚函数

```c++
class Example
{
    public:
    	virtual void print() = 0;
}
```

